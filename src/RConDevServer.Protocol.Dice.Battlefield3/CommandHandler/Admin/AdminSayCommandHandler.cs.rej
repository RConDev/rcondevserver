--- AdminSayCommandHandler.cs
+++ AdminSayCommandHandler.cs
@@ -4,6 +4,7 @@
 namespace RConDevServer.Protocol.Dice.Battlefield3.CommandHandler.Admin
 {
     using Common;
+    using Event;
 
     public class AdminSayCommandHandler : ICanHandleClientCommands
     {
@@ -28,8 +30,9 @@
             switch (playerSubset.Type)
             {
                 case PlayerSubsetType.None:
                     responsePacket.Words.Add(Response.InvalidArguments);
-                    return true;
+                    result = true;
+                    break;
 
                 case PlayerSubsetType.All:
                     responsePacket.Words.Add(Response.Success);
@@ -33,9 +36,10 @@
 
                 case PlayerSubsetType.All:
                     responsePacket.Words.Add(Response.Success);
-                    return true;
+                    result = true;
+                    break;
 
                 case PlayerSubsetType.Team:
                     responsePacket.Words.Add(playerSubset.TeamId <= 16
                                                  ? Response.Success
                                                  : Response.InvalidTeam);
@@ -37,9 +41,10 @@
 
                 case PlayerSubsetType.Team:
                     responsePacket.Words.Add(playerSubset.TeamId <= 16
                                                  ? Response.Success
                                                  : Response.InvalidTeam);
-                    return true;
+                    result = true;
+                    break;
 
                 case PlayerSubsetType.Squad:
                     if (playerSubset.TeamId <= 16)
@@ -60,5 +66,6 @@
                         session.Server.PlayerList.Players.Any(x => x.Name == playerSubset.PlayerName)
                             ? Response.Success
                             : Response.PlayerNotFound);
-                    return true;
+                    result = true;
+                    break;
             }
